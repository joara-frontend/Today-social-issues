"use client";

interface MobileNavPanelProps {
  onClose: () => void;
}

// TODO: 직접 구현 — 모바일 내비게이션 패널 UI

export function MobileNavPanel({ onClose }: MobileNavPanelProps) {
  void onClose;
  return <div>TODO: 직접 구현 — MobileNavPanel</div>;
}
